import CardRedirectContent from './card-redirect-content'
import CardRedirectHeader from './card-redirect-header'
import CardRedirectRoot from './card-redirect-root'

const CardRedirect = {
  Root: CardRedirectRoot,
  Header: CardRedirectHeader,
  Content: CardRedirectContent
}

export { CardRedirect }
